using VTLTools;

namespace MergeAR
{
    public class FriendlyRangedCharacter : RangedCharacter
    {
        protected override void Start()
        {
            //if (GameManager.Instance.State == GameState.Run)
            //    RunToBattlefield();
        }
    }
}